# تقرير التحقق — DTDC Display 5.2.0 Premium Stable

## قاعدة النسخة

- المصدر البصري والعملي الأساسي: `2.4.0-clinic-ui` المرسل من المستخدم.
- تم الحفاظ على واجهة العيادة المعتمدة وخط IBM Plex Sans Arabic والهوية الزجاجية الداكنة.
- نُقلت إليها إضافات الربط وعرض الخطة فقط، ولم تُستبدل بواجهة Display الأحدث غير المعتمدة.

## نتيجة البناء

- JDK: Temurin 17.
- Gradle: 8.11.1.
- Android Gradle Plugin: 8.9.2.
- Kotlin: 2.1.20.
- compileSdk / targetSdk: 35.
- minSdk: 23.
- النتيجة: `BUILD SUCCESSFUL`.
- المهام: 35 مهمة ناجحة.
- نجحت `compileDebugKotlin`.
- نجحت `processDebugManifest` و`mergeDebugResources`.
- نجحت `packageDebug` و`assembleDebug`.

ظهر تحذيران فقط عن واجهة fullscreen قديمة موروثة من النسخة الأساسية، وهما تحذيران غير مانعين للبناء أو التشغيل.

## فحوص APK

- package: `com.dentalchain.display`.
- versionCode: `52`.
- versionName: `5.2.0-premium-stable`.
- حجم APK التجريبي: نحو 17 MB.
- تحقق توقيع debug بنظامي v1 وv2.

ملف APK التجريبي موقّع بمفتاح debug جديد. إذا كان الجهاز يحتوي نسخة موقعة بمفتاح مختلف، يجب عدم استخدام
`adb install -r` عليها؛ إما تُزال النسخة القديمة أولاً أو يُعاد توقيع هذا الإصدار بالمفتاح السابق.

## فحوص الدمج

- WebSocket طويل العمر من دون مهلة قراءة قصيرة.
- فحص شبكة سريع بعميل منفصل ومهلة قصيرة.
- `display_ready` عند الاتصال.
- ACK للرسائل المهمة.
- تجاهل استدعاءات الفشل المتأخرة من اتصال قديم كي لا تقطع اتصالاً جديداً ناجحاً.
- إعادة محاولة الاتصال تلقائياً.
- قبول جلسة مريض جديدة وعدم افتراض الجنس للمرضى القدامى.
- الاسم الأول للترحيب، والاسم الكامل داخل خطة العلاج.
- عرض الخطة القصصي: بانوراما، تراكم مجموعات، مرحلة مفردة، ثم خاتمة خضراء.
- فتح DTDC GAMES كتطبيق مستقل وإعادة Display إلى الواجهة عند إرسال محتوى آخر.
- لا توجد لعبة داخلية قديمة.
- لا يوجد Workflow قديم داخل `.github/workflows`.
- تحتوي الحزمة Gradle Wrapper كاملاً، كما أن Workflow الحالي يبني بGradle 8.11.1 مباشرة ولا يعتمد على ملف wrapper ناقص.

## ما يحتاج تجربة على الجهاز

البناء والفحوص البرمجية اكتملت، لكن يلزم اختبار عملي على Android TV/الشاشة مع Controller 2.6.1 أو الأحدث للتأكد من:

- سرعة الاقتران على شبكة العيادة الفعلية.
- استقبال صورة المريض وحركات التكبير والتدوير.
- انتقال خطوات الخطة بالريموت والكونترولر.
- فتح DTDC GAMES والعودة منه.
